#!/usr/bin/env bash
# Criado em: 24/09/2026 16:54
# Modificado em: 25/09/2026 13:20
#
# Atalho para `praxisforge library publish` (feature 009, constituição Princípio VI).
# Repassa todos os argumentos e devolve o mesmo código de saída.
# Uso: scripts/publish-library (--type <tipo> <nome> | --all) --target <pasta-do-projeto> [--mode copy|symlink] [--prune]
set -euo pipefail

cd "$(dirname "${BASH_SOURCE[0]}")/.."
exec uv run praxisforge library publish "$@"
